% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drawSEM.R
\name{setManifestLatent}
\alias{setManifestLatent}
\title{Set or Clear Manifest/Latent Status of a Variable Node}
\usage{
setManifestLatent(graphModel, nodeId, value = NULL)
}
\arguments{
\item{graphModel}{A GraphModel object}

\item{nodeId}{Character; the id or label of a single variable node}

\item{value}{One of \code{"manifest"}, \code{"latent"}, or \code{NULL}. \code{NULL} clears any
existing lock and reverts the node to path-based auto-inference.}
}
\value{
The modified \code{graphModel} object (invisibly).
}
\description{
\code{manifestLatent} is normally inferred from the model's structure (manifest
if the node has an incoming data path from a dataset node, latent
otherwise). This function sets an explicit \code{variableCharacteristics$manifestLatent}
tag that locks the node's classification, overriding that inference — or
clears an existing lock to revert the node to inference.
}
\details{
There is no way to change manifest/latent structure on an existing
\code{MxModel} — OpenMx exposes no API for it, \code{mxModel(m, manifestVars = ...)}
is additive and errors on overlap, \code{mxModel(m, latentVars = character(0))}
is a silent no-op, and direct slot assignment leaves the F matrix
unrebuilt (producing a model whose \code{manifestVars} lies, with the same
-2LL and no error). This function only edits the GraphModel's schema;
regenerating the \code{MxModel} via \code{as.MxModel(as.GraphModel(m, ...))} is what
actually rebuilds a consistent F matrix, dims, and variable sets.

Setting \code{value = "manifest"} requires the node to already have an incoming
data path from a dataset node (the same rule \code{\link[=inferManifestVariables]{inferManifestVariables()}}
uses) — promoting a node with no bound data raises a schema-level error
here, rather than surfacing later as OpenMx's opaque "manifest variables
... have not been found in the observed data".
}
