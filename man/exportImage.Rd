% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drawSEM.R
\name{exportImage}
\alias{exportImage}
\title{Export a model diagram to SVG, PNG, or PDF}
\usage{
exportImage(
  graphModel,
  file,
  format = tools::file_ext(file),
  width = NULL,
  height = NULL,
  pathLabels = c("neither", "labels", "values", "both"),
  showDatasets = TRUE,
  showConstants = TRUE,
  autoLayout = "full",
  timeout = 15,
  ...
)
}
\arguments{
\item{graphModel}{A \code{GraphModel}, or anything accepted by \code{\link[=as.GraphModel]{as.GraphModel()}}.}

\item{file}{Output file path. The format is inferred from its extension
unless \code{format} is supplied.}

\item{format}{One of \code{"svg"}, \code{"png"}, or \code{"pdf"}. Defaults to the
extension of \code{file}.}

\item{width, height}{Optional output size in pixels for \code{"png"} (passed to
\code{\link[rsvg:rsvg_png]{rsvg::rsvg_png()}}). Ignored for \code{"svg"} and \code{"pdf"}, which use the
diagram's intrinsic dimensions.}

\item{pathLabels}{Path-label style, one of \code{"neither"} (default), \code{"labels"},
\code{"values"}, or \code{"both"}. Mirrors the editor's \emph{Export Image} option.}

\item{showDatasets, showConstants}{Whether to include dataset nodes and the
constant (\code{"1"}) node in the export. Both default to \code{TRUE}, matching the
editor's \emph{Export Image} button.}

\item{autoLayout}{Passed to \code{\link[=plotGraphModel]{plotGraphModel()}} when building the page.
Defaults to \code{"full"} so a model without stored node positions is laid out.}

\item{timeout}{Maximum seconds to wait for the widget to initialise before
giving up. Default 15.}

\item{...}{Additional arguments passed to \code{\link[=plotGraphModel]{plotGraphModel()}} when building the
page. Rarely needed; do not pass \code{showDataPaths}/\code{showConstantPaths} here
(use \code{showDatasets}/\code{showConstants}).}
}
\value{
The output \code{file} path, invisibly.
}
\description{
Generates a complete SVG of a model using the same generator as the editor's
\emph{Export Image} button, then writes it to disk. SVG is written directly; PNG
and PDF are produced from the SVG with the \pkg{rsvg} package, so PDF output
is true vector graphics rather than a rasterised screenshot.
}
\details{
The SVG is produced by the widget's own JavaScript export generator running
in a headless Chrome session, so headless output matches the editor exactly.
A Chrome/Chromium install is therefore required (via the \pkg{chromote}
package); PNG and PDF additionally require \pkg{rsvg}. Node positions are read
from the model; any node without a stored position is laid out automatically
first, so a bare model still exports sensibly.
}
\examples{
\dontrun{
gm <- loadGraphModel("model.json")
exportImage(gm, "model.pdf")                 # vector PDF
exportImage(gm, "model.png", width = 1600)   # raster PNG
exportImage(gm, "model.svg")                 # raw SVG
}

}
\seealso{
\code{\link[=plotGraphModel]{plotGraphModel()}} for the interactive/embeddable widget.
}
