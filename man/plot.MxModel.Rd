% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drawSEM.R
\name{plot.MxModel}
\alias{plot.MxModel}
\title{Plot method for MxModel objects}
\usage{
\method{plot}{MxModel}(x, editable = NA, autoLayout = NA, ...)
}
\arguments{
\item{x}{An \code{MxModel} object (from OpenMx)}

\item{editable}{Logical or \code{NA}. Auto-detect editability if \code{NA} (default).
See \code{\link[=plotGraphModel]{plotGraphModel()}} for details.}

\item{autoLayout}{Character or \code{NA}. Node positioning strategy.
"full" forces RAMPath layout, "partial" applies to unpositioned nodes,
"none" skips layout, or \code{NA} for auto-detect (default).
See \code{\link[=plotGraphModel]{plotGraphModel()}} for details.}

\item{...}{Additional arguments passed to \code{\link[=plotGraphModel]{plotGraphModel()}}}
}
\value{
Invisibly returns the htmlwidget created by \code{\link[=plotGraphModel]{plotGraphModel()}}
}
\description{
S3 plot method that displays an OpenMx fitted model as an interactive graph.
The mxModel is converted to a GraphModel for visualization, showing the
final model structure and fitted parameter values.
}
\details{
Converts the mxModel to a GraphModel using \code{\link[=as.GraphModel]{as.GraphModel()}}, which extracts:
\itemize{
\item Model structure (manifest and latent variables, paths)
\item Fitted parameter values and fixed/free status
\item Model data if attached
\item Optimization settings
}

The resulting visualization shows the final fitted model structure.
}
\examples{
\dontrun{
# After fitting an OpenMx model
library(OpenMx)
fit <- mxRun(myModel)

# Plot the fitted model structure
plot(fit)

# With options
plot(fit, editable = FALSE, autoLayout = "full")
}

}
