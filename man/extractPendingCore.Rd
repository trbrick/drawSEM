% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{extractPendingCore}
\alias{extractPendingCore}
\title{Relocate Non-Core Elements into extensions$pendingCore}
\usage{
extractPendingCore(schema)
}
\arguments{
\item{schema}{The schema list}
}
\value{
The schema with unsupported elements moved to extensions$pendingCore
}
\description{
Moves elements that the core schema cannot represent (0-headed paths;
\code{linkFunction} / \code{operator} nodes) out of each model's core \code{nodes}/\code{paths}
and into \code{model$extensions$pendingCore}, preserving them verbatim for lossless
round-trip. The cleaned core then validates strictly and the rest of the model
still loads and renders. When a node is relocated, any path incident to it is
relocated too, so the cleaned core has no dangling \code{from}/\code{to} references.
}
\details{
Priors are intentionally NOT relocated: they are schema-valid core content
(\code{optimization.prior}) that the OpenMx backend simply does not apply. They are
the "not applicable" warning tier, handled by the converter, not pendingCore.
}
\keyword{internal}
