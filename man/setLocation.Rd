% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphTool.R
\name{setLocation}
\alias{setLocation}
\title{Set node positions in a GraphModel}
\usage{
setLocation(graphModel, nodeId, x, y)
}
\arguments{
\item{graphModel}{A \code{GraphModel} object to modify}

\item{nodeId}{Character vector of node IDs or labels to position}

\item{x}{Numeric vector of X coordinates}

\item{y}{Numeric vector of Y coordinates}
}
\value{
The modified \code{graphModel} object (invisibly).
}
\description{
Programmatically set node positions by directly updating each node's visual properties.
Follows standard R vectorization semantics with recycling rules.
}
\details{
\strong{Position Storage:}

Positions are stored directly in the schema as \code{node.visual.x} and \code{node.visual.y}
properties for each node, eliminating the need for a separate positions data.frame.
This provides a single point of access for schema round-tripping and export.

\strong{Vectorization:}

\code{setLocation()} uses R's standard recycling rules. Shorter vectors are
repeated to match the longest vector's length. All vector lengths must
be multiples of each other, or an error is raised.

Examples:
\itemize{
\item \code{setLocation(gm, "X", 10, 20)} → Single node at (10, 20)
\item \code{setLocation(gm, c("X", "Y"), 10, c(1, 5))} → X at (10, 1), Y at (10, 5)
(x recycled)
\item \code{setLocation(gm, c("X", "Y", "Z"), c(10, 20, 30), c(1, 2, 3))} →
X at (10, 1), Y at (20, 2), Z at (30, 3)
}

\strong{Non-Multiple Lengths:}

If vector lengths are not multiples of each other, an error is raised:
\itemize{
\item \code{setLocation(gm, c("X", "Y", "Z"), 10, c(1, 2))} → Error
(lengths 3, 1, 2 are not all multiples)
}

\strong{Invalid Nodes:}

If any nodeId doesn't exist in the schema, a warning is issued and that
node is skipped.
}
\examples{
# Single node
gm <- setLocation(gm, "X", 100, 200)

# Multiple nodes with vectorization
gm <- setLocation(gm, c("X", "Y", "Z"), c(10, 20, 30), c(1, 2, 3))

# Recycling behavior
gm <- setLocation(gm, c("X", "Y"), 10, c(1, 5))  # x recycled

}
