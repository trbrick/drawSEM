% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting.R
\name{generateData}
\alias{generateData}
\title{Simulate Data from a GraphModel}
\usage{
generateData(
  graphModel,
  nrows = NULL,
  backend = "OpenMx",
  returnModel = FALSE,
  model_id = NULL,
  onUnsupported = c("stop", "ignore"),
  ...
)
}
\arguments{
\item{graphModel}{A GraphModel object to simulate data from}

\item{nrows}{Integer; number of rows to simulate. Passed through to
mxGenerateData(); see its documentation for the default when omitted.}

\item{backend}{Character; which backend to simulate with. Currently only
\code{"OpenMx"} is implemented; see \code{\link[=runModel]{runModel()}} for the same convention.}

\item{returnModel}{Logical; if \code{FALSE} (default), returns the simulated
data as a data.frame -- mirroring \code{mxGenerateData()}'s own default. If
\code{TRUE}, returns a new GraphModel with the simulated data bound in as a
dataset node, ready to fit with \code{\link[=runModel]{runModel()}}, instead of the raw
data.frame.}

\item{model_id}{Character; which model to simulate from (defaults to first)}

\item{onUnsupported}{How to handle non-core \code{extensions$pendingCore} features:
"stop" (default) refuses with an error listing them; "ignore" builds a
reduced model that omits them (with a warning).}

\item{...}{Additional arguments passed to mxGenerateData()}
}
\value{
A data.frame of simulated data (\code{returnModel = FALSE}), or a new
GraphModel with the simulated data bound in (\code{returnModel = TRUE}).
With \code{returnModel = TRUE}, original graphModel is unchanged (R's
copy-on-modify semantics).
}
\description{
Wrapper around as.MxModel() + mxGenerateData() that simulates data
consistent with a GraphModel's structure and parameter values (fixed
values, or free parameters at their start values). This is the tool for
populating a data-less template/simulation model that runModel() refuses
to fit.
}
\examples{
\dontrun{
g <- as.GraphModel(schema)  # no dataset node -- a template model
df <- generateData(g, nrows = 200)

g_with_data <- generateData(g, nrows = 200, returnModel = TRUE)
g_fit <- runModel(g_with_data)
}

}
