% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GraphModel-class.R
\docType{class}
\name{GraphModel-class}
\alias{GraphModel-class}
\title{GraphModel S4 Class}
\description{
The \code{GraphModel} class is the central object for the OpenMx WebUI package. It stores:
\itemize{
\item \strong{schema:} The structural definition (nodes, paths, optimization parameters) as a list
\item \strong{data:} Named list of data.frames or file paths for datasets referenced in the schema
\item \strong{metadata:} UI state (node positions, colors, labels, unsupported features)
\item \strong{lastBuiltModel:} Cached mxModel object (or NULL if not yet built)
}

The schema is JSON-serializable and the source of truth. The GraphModel can be converted
transparently to backend-specific objects (OpenMx, lavaan, blavaan) without losing
structural information.
}
\details{
A schema-first representation of a statistical model (SEM, growth curve, multilevel, etc.)
that preserves structural information, UI metadata, and backend-specific configurations.

Typical workflow:
\enumerate{
\item Create a GraphModel from JSON schema: \code{g <- as.GraphModel(schema, data = list(...))}
\item Optionally inspect the schema: \code{schema(g)}, \code{data(g)}, \code{metadata(g)}
\item Build and run the model: \code{fit <- mxRun(g)}
\item Export for later use: \code{exportSchema(g, "model.json")}
}
}
\section{Slots}{

\describe{
\item{\code{schema}}{A list representing the graph schema. Must have \code{schemaVersion} and \code{models} keys.
See the graph.schema.json for full specification.}

\item{\code{data}}{A named list where names are dataset node IDs and values are data.frames or
file paths (character strings). Used by the converter to build mxData objects.}

\item{\code{metadata}}{A list for storing UI state and other non-schema information. May contain:
\itemize{
\item \code{unsupported}: A list of features not yet supported by the converter
\item \code{positions}: Visual node positions (for round-tripping through the GUI)
\item \code{colors}, \code{labels}: UI styling information
}}

\item{\code{lastBuiltModel}}{The cached backend-specific model object (typically an mxModel). Set when
\code{as.MxModel()} or \code{mxRun()} is called. NULL if not yet built or if schema
has been modified since last build.}

\item{\code{dataConnections}}{A list tracking data connection state for each dataset in the schema.
Names are dataset labels, values are lists with:
\itemize{
\item \code{status}: "eager", "lazy", or "unconnected"
\item \code{filepath}: Path to the file (if status is "eager" or "lazy")
\item \code{columns}: Column names from file metadata (for validation)
}}
}}

\examples{
\dontrun{
# Create from JSON schema
schema_list <- list(
  schemaVersion = 1,
  models = list(
    model1 = list(
      nodes = list(...),
      paths = list(...)
    )
  )
)

g <- as.GraphModel(schema_list, data = list(mydata = my_df))

# Inspect structure
schema(g)
data(g)
metadata(g)

# Build and run
fit <- mxRun(g)
summary(fit)
}

}
\seealso{
\itemize{
\item \code{\link[=as.GraphModel]{as.GraphModel()}} for creating instances
\item \code{\link[=as.MxModel]{as.MxModel()}} for converting to OpenMx
\item \code{\link[=schema]{schema()}}, \code{\link[=data]{data()}}, \code{\link[=metadata]{metadata()}} for accessors
}
}
