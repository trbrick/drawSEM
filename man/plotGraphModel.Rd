% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drawSEM.R
\name{plotGraphModel}
\alias{plotGraphModel}
\title{Plot a GraphModel object interactively}
\usage{
plotGraphModel(
  graphModel,
  editable = NA,
  layout = NULL,
  forceLayout = FALSE,
  autoLayout = NA,
  showDataPaths = FALSE,
  showConstantPaths = TRUE,
  pathLabelFormat = "neither",
  width = NULL,
  height = NULL,
  elementId = NULL,
  ...
)
}
\arguments{
\item{graphModel}{A \code{GraphModel} object to visualize}

\item{editable}{Logical or \code{NA} (default). Controls whether users can edit
the graph interactively.
\itemize{
\item \code{NA}: Auto-detect based on context (interactive session or Shiny app)
\item \code{TRUE}: Force editable regardless of context
\item \code{FALSE}: Force read-only regardless of context
}}

\item{layout}{Character. Node positioning strategy:
\itemize{
\item \code{"auto"} (default): Compute positions if missing; use existing if present
\item \code{"provided"}: Positions must exist (error if missing, forces explicit
user specification)
}}

\item{forceLayout}{Logical. If \code{TRUE}, re-compute layout even if positions
exist. Overrides \code{layout} parameter. Default: \code{FALSE}}

\item{showDataPaths}{Logical. If \code{TRUE}, show dataset nodes in the graph.
Default: \code{FALSE}}

\item{showConstantPaths}{Logical. If \code{TRUE}, show paths connecting to
constant nodes (means). Default: \code{TRUE}}

\item{pathLabelFormat}{Character. Controls path parameter display:
\itemize{
\item \code{"neither"} (default): No labels
\item \code{"labels"}: Show parameter labels only
\item \code{"values"}: Show estimated/fixed values
\item \code{"both"}: Show labels and values
}}

\item{width}{Numeric or character. Widget width (default: \code{NULL} for auto)}

\item{height}{Numeric or character. Widget height (default: \code{NULL} for auto)}

\item{elementId}{Character. Optional HTML element ID for advanced use}

\item{...}{Additional arguments passed to \code{htmlwidgets::createWidget()}}
}
\value{
Invisibly returns an htmlwidget object that can be captured with
\verb{<-} assignment. Displays the graph in the output device (viewer, console,
document, etc.)
}
\description{
Renders a \code{GraphModel} object as an interactive graph widget using the visual
web tool. Automatically detects editability based on context (interactive R
session, Shiny app, etc.) and manages node positioning with layout control.
}
\details{
\strong{Auto-Detect Logic:}

When \code{editable = NA} (default), the function auto-detects whether the graph
should be editable:
\itemize{
\item Returns \code{TRUE} if running in interactive R session (console, RStudio)
that is not rendering a document (Quarto, RMarkdown PDF)
\item Returns \code{TRUE} if running in a Shiny app
\item Returns \code{FALSE} if in a non-interactive context (batch script, knitting)
\item Shows a message in interactive mode explaining editability status
}

\strong{Layout Computation:}

The \code{autoLayout} parameter controls whether the widget computes node positions
using the RAMPath algorithm:
\itemize{
\item \code{NA} (default): Auto-detect. If displayed nodes lack positions, computes them
in the widget. Otherwise, uses existing positions. Shows helpful message.
\item \code{"full"}: Always compute full layout in widget, even if positions exist
\item \code{"partial"}: Compute layout for missing nodes only (future use)
\item \code{"none"}: Never compute; assume positions are pre-set via setLocation()
or will error if nodes have no positions
}

\strong{Position Storage:}

Node positions are stored directly in each node's \code{visual} property
(as \code{node.visual.x} and \code{node.visual.y}). This provides a single point
of access for schema round-tripping and export.

\strong{Two-Phase Workflow:}

Recommended workflow for reproducible plotting:
\enumerate{
\item Create and explore model interactively: \code{plotGraphModel(gm)}
(widget will auto-compute layout if missing)
\item Once satisfied with layout, save positions programmatically:
\code{gm <- setLocation(gm, nodeIDs, x_positions, y_positions)}
\item For future sessions, reuse layout: \code{plotGraphModel(gm, autoLayout = "none")}
}
}
\examples{
# Minimal example with auto-layout
schema <- list(
  schemaVersion = 1,
  models = list(
    model1 = list(
      nodes = list(
        list(id = "X", label = "X", type = "variable"),
        list(id = "Y", label = "Y", type = "variable")
      ),
      paths = list(
        list(from = "X", to = "Y", arrows = 1)
      )
    )
  )
)

gm <- as.GraphModel(schema)

# Plot with auto-detected editability and auto layout
plotGraphModel(gm)

# Force non-interactive display
plotGraphModel(gm, editable = FALSE)

# Always compute layout
plotGraphModel(gm, autoLayout = "full")

# Never compute layout (assume positions exist)
plotGraphModel(gm, autoLayout = "none")

}
