% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drawSemHints-class.R
\docType{class}
\name{drawSemHints-class}
\alias{drawSemHints-class}
\title{drawSEM Layout/Provenance Hints}
\description{
Escrows drawSEM state an \code{mxModel} cannot represent -- node/path visual
positions, extra constant/dataset nodes, and exporter provenance -- so a
model built in drawSEM, run in OpenMx, and converted back with
\code{as.GraphModel()} recovers layout and identity instead of falling back to
auto-layout and the hardcoded constant/dataset labels \code{"1"}/\code{"data"}.

Stored, by convention, at \code{m@options$drawSemHints} -- an option key OpenMx
does not recognize, and therefore never inspects or validates. Verified (on
OpenMx 2.22.11) to survive \code{mxRun()}, \code{mxTryHard()}, \code{mxAutoStart()},
\code{omxSetParameters()}, \code{mxModel()} edits, \code{mxRename()}, \code{saveRDS()}, and
multigroup submodels, \code{identical()} each time, with no effect on the fit.
\code{mxOption(model, key, value, reset = TRUE)} is the one operation that drops
it, because OpenMx resets the entire options list before applying the new
key; treat a model with no \code{drawSemHints} the same as one that never had
any -- auto-layout, a single constant node labeled \code{"1"}, a single dataset
node labeled \code{"data"}.

The class is a proper S4 object rather than a plain list so that
\code{length(hints) == 1} by construction: \code{mxModel}'s \code{show()} renders the
options list via \code{omxQuotes()}, which errors on any option value whose
top-level elements have length > 1 (\code{if} on a length > 1 condition is an
error since R 4.2). \code{omxQuotes()} has an explicit S4 branch that renders
just the class name, so \code{show(someModel)} never touches \code{object@model}.

Deliberately holds only what the schema fragment cannot re-derive on its
own: no structure hash (node/path matching in \code{as.GraphModel()} handles
added, removed, and incomplete nodes; explicit OpenMx-side edits are meant
to show up), no constant-node value assignments (already encoded by the
fragment's own paths, e.g. \code{from: "1b"}), no data fingerprint (belongs
inside \code{model$nodes[[i]]$datasetSource}, beside \code{md5}).
}
\section{Slots}{

\describe{
\item{\code{schemaVersion}}{Numeric; the schema version of \code{model}, for migration
dispatch if the fragment shape changes later.}

\item{\code{model}}{The model's schema fragment (\code{label}, \code{nodes}, \code{paths},
\code{optimization}), with every dataset node's \code{datasetSource$object}
stripped -- embedded data would otherwise dominate the escrowed size.}

\item{\code{origin}}{Character; the exporter stamp, e.g. \code{"drawSEM 0.1.0"}.}

\item{\code{exported}}{Character; ISO-8601 UTC timestamp of when the hints were
written.}
}}

\keyword{internal}
