% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{loadGraphModel}
\alias{loadGraphModel}
\title{Load GraphModel from JSON File}
\usage{
loadGraphModel(filepath, data = NULL, datapath = getwd(), dataName = NULL)
}
\arguments{
\item{filepath}{Path to JSON file}

\item{data}{Optional. Can be a named list of data.frames to bind to the model.
Pass \code{list()} (empty list) to skip automatic data loading from schema.}

\item{datapath}{Path to search for CSV files specified in the schema.
Defaults to current working directory. Data files are eagerly loaded if found
and columns match the schema specification.}
}
\value{
A GraphModel object with data connections established
}
\description{
Load a GraphModel from a previously exported JSON schema file.
}
\details{
Data loading strategy:
\enumerate{
\item If \code{data} is provided as a named list, those datasets are bound immediately
\item The function looks for CSV files referenced in the schema (via \code{datasetFile.fileName})
in the \code{datapath} directory
\item Found files are validated (column names must match the schema) and eagerly loaded
\item If a file is not found or columns don't match, it's marked for lazy loading
(will be attempted when \code{as.MxModel()} is called)
\item When building a model with \code{as.MxModel()}, any unconnected datasets will be
lazy-loaded from the schema-specified file path
}
}
\examples{
\dontrun{
# Load with automatic file discovery
g <- loadGraphModel("model.json")

# Load with explicit data
g <- loadGraphModel("model.json", data = list(sample = my_df))

# Load from custom path
g <- loadGraphModel("model.json", datapath = "~/data/myproject")

# Skip automatic data loading
g <- loadGraphModel("model.json", data = list())
}

}
