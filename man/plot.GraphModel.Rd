% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graphTool.R
\name{plot.GraphModel}
\alias{plot.GraphModel}
\title{Plot method for GraphModel objects}
\usage{
\method{plot}{GraphModel}(x, editable = NA, layout = "auto", ...)
}
\arguments{
\item{x}{A \code{GraphModel} object}

\item{editable}{Logical or \code{NA}. Auto-detect editability if \code{NA} (default).
See \code{\link[=plotGraphModel]{plotGraphModel()}} for details.}

\item{layout}{Character. Node positioning strategy ("auto" or "provided").
See \code{\link[=plotGraphModel]{plotGraphModel()}} for details.}

\item{...}{Additional arguments passed to \code{\link[=plotGraphModel]{plotGraphModel()}}}
}
\value{
Invisibly returns the htmlwidget created by \code{\link[=plotGraphModel]{plotGraphModel()}}
}
\description{
S3 plot method that displays a GraphModel as an interactive graph. Provides
a convenient interface via R's standard \code{plot()} function.
}
\details{
This is a convenience method that delegates to \code{\link[=plotGraphModel]{plotGraphModel()}}. All
parameters for controlling layout, editability, and display are available
through the \code{...} argument.
}
\examples{
# Create a simple GraphModel
schema <- list(
  schemaVersion = 1,
  models = list(
    model1 = list(
      nodes = list(
        list(id = "X", label = "X", type = "variable"),
        list(id = "Y", label = "Y", type = "variable")
      ),
      paths = list(
        list(from = "X", to = "Y", arrows = 1)
      )
    )
  )
)

gm <- as.GraphModel(schema)

# Plot using the S3 method
plot(gm)

# With options
plot(gm, editable = FALSE, forceLayout = TRUE)

}
