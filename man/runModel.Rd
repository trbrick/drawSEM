% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitting.R
\name{runModel}
\alias{runModel}
\title{Fit a GraphModel}
\usage{
runModel(
  graphModel,
  backend = "OpenMx",
  model_id = NULL,
  silent = FALSE,
  intervals = FALSE,
  unsafe = FALSE,
  onUnsupported = c("stop", "ignore"),
  ...
)
}
\arguments{
\item{graphModel}{A GraphModel object to fit}

\item{backend}{Character; which backend to fit with. Currently only
\code{"OpenMx"} is implemented; other values from the schema's
\code{fitResults.backend} enum (\code{"lavaan"}, \code{"blavaan"}, \code{"other"}) are
accepted as valid input but raise a "not yet implemented" error, so a
typo is caught the same way an unrecognized value would be.}

\item{model_id}{Character; which model to fit (defaults to first)}

\item{silent}{Logical; if TRUE, suppress mxRun() status messages}

\item{intervals}{Logical; if TRUE, compute confidence intervals}

\item{unsafe}{Logical; if TRUE, ignore errors during optimization}

\item{onUnsupported}{How to handle non-core \code{extensions$pendingCore} features:
"stop" (default) refuses with an error listing them; "ignore" fits a reduced
model that omits them (with a warning), leaving them in the schema.}

\item{...}{Additional arguments passed to mxRun()}
}
\value{
A new GraphModel with fitted parameters and fit metadata stored in
the schema's provenance section. Original graphModel is unchanged
(R's copy-on-modify semantics).
}
\description{
Wrapper around as.MxModel() + mxRun() that fits a GraphModel and
stores results back in the schema.
}
\details{
Workflow (backend = "OpenMx"):
\enumerate{
\item Convert GraphModel → mxModel (as.MxModel)
\item Fit with mxRun(..., silent=silent, intervals=intervals, unsafe=unsafe, ...)
\item Extract fitted parameters, standard errors, fit value, etc.
\item Store in schema$models[\link{model_id}]$provenance$fitResults
\item Mark with current structure hash for staleness detection
\item Return new GraphModel with $lastBuiltModel set to fitted mxModel
}
}
\examples{
\dontrun{
g <- as.GraphModel(schema, data = list(mydata = df))
g_fit <- runModel(g, silent = TRUE)

# Access fit results
loglik(g_fit)
coef(g_fit)
summary(g_fit)
}

}
